<%= form_with(model: tariff) do |form| %>
  <% if tariff.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tariff.errors.count, "error") %> prohibited this tariff from being saved:</h2>

      <ul>
        <% tariff.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_area :title %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.label :active_tariff_id, style: "display: block" %>
    <%= form.number_field :active_tariff_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
